class BlogDataConfig {
  String title;
  String id;
  String imageUrl;
  bool favourite;

  BlogDataConfig(
      {required this.title,
        required this.id,
        required this.imageUrl,
        required this.favourite,
      });
}