class DetailsTypeConfig {
  String title;
  String orderId;
  String amount;
  DateTime date;
  String place;

  DetailsTypeConfig(
      {required this.title,
        required this.orderId,
        required this.amount,
        required this.date,
        required this.place,
      });
}